# WebSockets + NodeJs/Socket.io + React Performance Test

Requirements:
> Node.js

Project setup:
```$ npm install```

Build:
```$ npm run build```

Watch (for development):
```$ npm run watch```

Run the web site:
```$ npm start```

Browse:
```http://localhost:3000```